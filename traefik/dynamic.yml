http:
  routers:    
    frontend:
      rule: "PathPrefix(`/frontend`)"
      service: frontend
    api:
      rule: "PathPrefix(`/api`)"
      service: api
  services:    
    frontend:
      loadBalancer:
        servers:
        - url: http://frontend:3000
    api:
      loadBalancer:
        servers:
        - url: http://api:5000